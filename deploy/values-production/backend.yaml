image:
  tag: 0d894ea4

config:
  # REQUIRED — your production domain(s), comma-separated.
  # Example: "https://app.example.com"
  corsOrigins: ""
  # REQUIRED — async SQLAlchemy URL to the information-system Postgres.
  # Example: "postgresql+asyncpg://diploma:PASS@appdb-postgresql.db.svc:5432/diploma"
  databaseUrl: ""

# REQUIRED — JWT signing secret. Provide via a sealed secret / external secret;
# do not commit the real value here.
jwtSecret: ""
