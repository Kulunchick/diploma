apiVersion: v2
name: redis
description: Redis wrapper — pulls bitnami/redis as a sub-chart
type: application
version: 0.1.0
dependencies:
  - name: redis
    version: ">=20.0.0"
    repository: https://charts.bitnami.com/bitnami
