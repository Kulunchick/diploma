apiVersion: v2
name: temporal
description: Temporal wrapper — pulls temporalio/temporal 0.74.0 (last bundled-Cassandra release)
type: application
version: 0.1.0
dependencies:
  - name: temporal
    version: "0.74.0"
    repository: https://go.temporal.io/helm-charts
