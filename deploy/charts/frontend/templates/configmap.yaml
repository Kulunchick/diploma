apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}-config
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
data:
  APP_API_BASE_URL: {{ .Values.config.apiBaseUrl | quote }}
  APP_USE_TEMPORAL_API: {{ .Values.config.useTemporalApi | quote }}
