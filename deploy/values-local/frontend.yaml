image:
  tag: d8fad2a0  # set by CI write-back on every deploy (8-char git SHA)
  pullPolicy: IfNotPresent

replicaCount: 1

config:
  apiBaseUrl: "http://localhost:8000/api"

ingress:
  enabled: false
