# Build context = ./apps/ui for the ui image. node_modules and dist are
# rebuilt inside the image; keeping them out avoids host/platform mismatch
# and a slow build context upload.
node_modules
dist
.vite
**/__pycache__
**/*.log
.DS_Store
Thumbs.db
