server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing: any unknown path returns index.html so React Router works
    location / {
        try_files $uri $uri/ /index.html;
    }
}
